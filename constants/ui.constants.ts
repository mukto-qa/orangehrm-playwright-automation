export const LOGIN_PAGE_DATA = {
  HEADER_TEXT: "Login",
  ERROR_MESSAGES: {
    INVALID_CREDENTIALS: "Invalid credentials",
    USERNAME_REQUIRED_TEXT: "Required",
    PASSWORD_REQUIRED_TEXT: "Required",
  },
} as const;

export const DASHBOARD_PAGE_DATA = {
  HEADER_TEXT: "Dashboard",
} as const;